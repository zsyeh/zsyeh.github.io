---
haloId: "6d0837a3-97d6-463e-96d8-43791e0ba106"
author: "YangLuoNou"
source: "Halo"
title: "[实用]Ubuntu修改(创建)swap方法"
slug: "d37fda98-fed8-479b-96a0-3a21ec5ed0ce"
description: "新增或替换一个更大的 swap 文件 查看当前 swap 情况 sudo swapon --show\nfree -h\n 关闭所有 swap sudo swapoff -a\n 创建一个新的 swap 文件 使用 fallocate（快速）： sudo fallocate -l 4G /swapfile"
pubDate: "2025-11-29T16:16:06.806457834Z"
updatedDate: "2025-11-29T16:27:56.563935498Z"
cover: "/halo-assets/image-INHe.png"
categories: ["飞控"]
tags: ["实用","wsl","Linux","机载电脑"]
pinned: false
haloUrl: "https://dxlab.ehzsy.space/archives/d37fda98-fed8-479b-96a0-3a21ec5ed0ce"
---

## 新增或替换一个更大的 swap 文件

1. **查看当前 swap 情况**  
   ```bash
   sudo swapon --show
   free -h
   ```
2. **关闭所有 swap**  
   ```bash
   sudo swapoff -a
   ```
3. **创建一个新的 swap 文件**  
   - 使用 `fallocate`（快速）：
     ```bash
     sudo fallocate -l 4G /swapfile
     ```
   - 如果 `fallocate` 不可用，使用 `dd`：
     ```bash
     sudo dd if=/dev/zero of=/swapfile bs=1M count=4096 status=progress
     ```
4. **设置正确权限**  
   ```bash
   sudo chmod 600 /swapfile
   ```
5. **格式化并启用 swap**  
   ```bash
   sudo mkswap /swapfile
   sudo swapon /swapfile
   ```
6. **验证生效**  
   ```bash
   swapon --show
   free -h
   ```
7. **开机自动挂载**  
   在 `/etc/fstab` 末尾添加：  
   ```text
   /swapfile none swap sw 0 0
   ```
8. **（可选）调整 swappiness**  
   系统默认 `vm.swappiness=60`，可调小：  
   ```bash
   echo 'vm.swappiness=10' | sudo tee /etc/sysctl.d/99-swappiness.conf
   sudo sysctl --system
   ```
